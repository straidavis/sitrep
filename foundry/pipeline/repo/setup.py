from setuptools import setup, find_packages

setup(
    name='sitrep_transforms',
    version='1.0.0',
    description='SITREP Data Pipeline',
    packages=find_packages(),
    install_requires=[],
)
